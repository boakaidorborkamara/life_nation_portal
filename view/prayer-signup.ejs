<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Life Church Portal</title>
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/style.css">
</head>

<body>
    <!--navigation bar-->
    <header class="bg-light">
        <section class=" p-2" id="nav-area">
            <div class="container">
                <nav class="navbar navbar-expand-lg navbar-light">
                    <div class="container-fluid">
                        <a class="navbar-brand fw-bolder fs-3" href="index.html">
                            <!-- <i class="fas fa-warehouse text-red"></i> -->
                            LIFE CHURCH
                        </a>
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                    </button>
                        <div class="collapse navbar-collapse" id="navbarSupportedContent">
                            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                                <li class="nav-item ps-2">
                                    <a class="nav-link active fw-bold py-3" aria-current="page" href="#"></a>
                                </li>
                                <li class="nav-item ps-2">
                                    <a class="nav-link active fw-bold py-3" aria-current="page" href="#">About</a>
                                </li>
                                <li class="nav-item ps-2">
                                    <a class="nav-link active fw-bold py-3" aria-current="page" href="#">Contact Us</a>
                                </li>
                            </ul>

                        </div>
                    </div>
                </nav>
            </div>
        </section>
    </header>
    <main>
        <section class="text-center text-light">

            <h1 class="mt-5 fw-bolder">
                LET'S PRAY TOGETHER.
            </h1>
            <p class="mb-3">We Are One Nation - God's People - Life Nation!</p>
        </section>
        <section class="d-lg-flex justify-content-center align-items-center">
            <form class="bg-light  p-4" id="signup-form" method="" action="">
                <div class="mb-3">
                    <input type="text" placeholder="First Name*" required class="form-control" id="first-name">
                </div>
                <div class="mb-3">
                    <input type="text" placeholder="Middle Name" class="form-control" id="middle-name">
                </div>
                <div class="mb-3">
                    <input type="text" required placeholder="Last Name*" class="form-control" id="last-name">
                </div>
                <div class="mb-3">
                    <input type="text" placeholder="WhatsApp Number" class="form-control" id="WhatsApp_number">
                </div>
                <div class="mb-3">
                    <input type="text" placeholder="Phone Number" class="form-control" id="phone_number">
                </div>
                <div class="mb-3">
                    <input type="text" placeholder="Facebook Handle" class="form-control" id="facebook">
                </div>
                <div>
                    <select class="form-control" name="" id="available-time">
                        <option value="Time Available To Pray" selected  disabled>Time Available To Pray</option>
                        <option value="">Evening</option>
                        <option value="">Morning</option>
                    </select>
                </div>
                <!-- <div class="btn-group">
                    <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                      Time Available
                    </button>
                    <ul class="dropdown-menu">
                        ...
                    </ul>
                </div> -->
                <button type="submit" id="submit-btn" class="btn btn-primary mt-3">Signup</button>
                <!-- <input type="submit" id="submit-btn" class="btn btn-primary mt-3"> -->
            </form>
        </section>
    </main>

    <script>
        let signup_form = document
        let first_name = document.getElementById('first-name');
        let middle_name = document.getElementById('last-name');
        let last_name = document.getElementById('last-name');
        let WhatsApp_number = document.getElementById('WhatsApp_number');
        let phone_number = document.getElementById('phone_number');
        let facebook = document.getElementById('facebook');
        let available_time = document.getElementById('available-time');
        let submit_btn = document.getElementById('submit-btn');


        //get user data from frontend
        function getFrontendData() {
            let frontend_data = {
                first_name: first_name.value,
                middle_name: middle_name.value,
                last_name: last_name.value,
                WhatsApp_number: WhatsApp_number.value,
                phone_number: phone_number.value,
                facebook: facebook.value,
                available_time: available_time.value
            }

            return frontend_data;
        }

        //validate frontend data
        function validateFrondendData(data) {
            if (data.first_name === "") {
                first_name.style.border = "1px solid red";
                alert("First Name field is required");
                return 1;
            } else if (data.last_name === "") {
                last_name.style.border = "1px solid red";
                alert("Last Name field is required");
                return 1;
            } else if (data.available_time === "Time Available To Pray") {
                available_time.style.border = "1px solid red";
                alert("Please select an Available Prayer Time");
                return 1;
            } else {
                return 0;
            }
        }


        //send data to the backend
        function sendDataToBackend(user_data, url) {
            console.log("Sending data...........");

            fetch(url, {

                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify(user_data)
                })
                .then(response => response.json())
                .then(data => data)
        }


        //add functionality to submit btn
        signup_form.addEventListener('submit', () => {

            //remove default behaviour from form 
            event.preventDefault()


            //data from frontend
            let user_data = getFrontendData();
            console.log(user_data);

            //validate data
            let validation_status = validateFrondendData(user_data);

            if (validation_status === 0) {
                sendDataToBackend(user_data, "/try");
            } else {
                console.log("Error in validation");
            }
        })
    </script>
</body>

</html>